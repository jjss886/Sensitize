import React, { Component } from "react";
import { connect } from "react-redux";

class ChartWrapperFishEye extends Component {
  render() {
    return <div>TESTING</div>;
  }
}

export default connect()(ChartWrapperFishEye);
